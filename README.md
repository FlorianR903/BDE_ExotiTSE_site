Before launch or deploy the app, create your .env.local with environment variables inside !
Then, write these commands in your terminal :
  -> npm install
  -> npm install @stripe/react-stripe-j
  
Launch overview --> Click on the link (on the right of the page) in About section !
